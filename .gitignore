# A white list .gitignore:
# Ignore everything, then add specific subtrees/files
# Ignore everything
*
# But descend into directories
!*/
# Recursively allow files under subtree
!/src/**
# You can be specific with these rules
#!/some/other/deep/path/**
!.gitignore
!build.gradle
!README.md

